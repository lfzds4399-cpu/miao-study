version: '3.8'

services:
  app:
    build: .
    ports:
      - "80:8000"
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    volumes:
      - ./study.db:/app/study.db
    restart: always
